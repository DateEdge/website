<div class="page-header">
  <% if @query %>
    <h1>
      <% if %w(diets genders straightedgeness).include? @column %>
        <%= link_to @column.capitalize, send("#{@column}_path") %> :
      <% else %>
        Search :
      <% end %>

      <%= @query.capitalize %>
      <small>(<%= @total %>)</small>
    </h1>
  </div>
  <% else %>
    <h1>Everyone</h1>
  <% end %>
</div>

<%= render "/users/listing" %>

<%= render "/users/pagination" %>
