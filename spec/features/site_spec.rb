require 'spec_helper'

describe "tests user experience", js: true do
  let(:bookis) { create(:bookis) }
  let(:shane)  { create(:shane)  }
  
  it "does views users, crushes, bookmarks, and stuff"
end